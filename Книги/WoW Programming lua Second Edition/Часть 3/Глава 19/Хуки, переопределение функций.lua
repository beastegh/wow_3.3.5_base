📘 Изменение существующего поведения с помощью перехвата функций

  Большинство аддонов создаются как автономные дополнения к пользовательскому интерфейсу по умолчанию. Они используют предоставленный API, а также фреймы и функциональность стандартного интерфейса. Однако иногда бывает разумно изменить уже существующее поведение интерфейса. Один из способов сделать это — использовать технику, известную как перехват функций [function hooking].

  В этой главе вводится понятие перехвата функций, включая правила, которым следует придерживаться, и распространённые ошибки, которых стоит избегать.
  
📘 Что такое перехват функций?

  “Перехват функции“ — это по сути красивый способ сказать “изменение поведения функции“. Как и любую другую переменную в Lua, функцию можно заменить на созданную вами новую версию. Тогда при попытке вызова этой функции будет использована ваша версия.
  
  Однако более точное значение перехвата — это когда оригинальная функция все равно вызывается, по крайней мере если перехват реализован правильно.
  
  👉 Перехваты функций применяются для изменения поведения существующих функций одним из способов:
    Изменение аргументов или возвращаемых значений оригинальной функции.
    Блокировка выполнения оригинальной функции.
    Выполнение дополнительных действий при каждом вызове функции.
  
  👉 Пример:
    Следующий код добавляет временную метку ко всем сообщениям, выводимым в стандартное окно чата через функцию ChatFrame1:AddMessage():
   
    local origAddMessage = ChatFrame1.AddMessage
    function ChatFrame1.AddMessage(self, text, ...)
      local timestamp = date("%X")
      text = string.format("%s %s", timestamp, text)
      return origAddMessage(self, text, ...)
    end
    
  Первая строка сохраняет оригинальную функцию AddMessage в локальную переменную origAddMessage. Затем создаётся новая функция, которая добавляет к тексту текущее время и вызывает оригинальную функцию, передавая изменённый текст.
  
  👉 Обратите внимание на два важных момента:
    Новая функция принимает переменное число аргументов (...). Это позволяет вам сосредоточиться только на первых параметрах, а также гарантирует, что при изменении количества или порядка параметров оригинальной функции ваш перехват продолжит работать правильно, если позиция текста не изменилась.

    Результат вызова оригинальной функции возвращается. Это может показаться лишним, так как на момент написания AddMessage ничего не возвращает, но такая практика защищает ваш код на случай, если в будущем функция начнёт возвращать значение.
  
  В целом, если вы перехватываете функцию, имейте в виду, что это могут делать и другие, поэтому ваша замена должна вести себя максимально похоже на оригинал, чтобы не нарушать работу остальных частей кода.
  
📘 Изменение возвращаемых значений

  Существует два основных типа перехвата функций. Пример из предыдущего раздела называется pre-hook — когда основное действие перехвата происходит до вызова оригинальной функции. Это видно в коде, где вызов и возврат из origAddMessage происходят в самом конце.
  
  Post-hook — наоборот, выполняется после того, как оригинальная функция вернула результат. Такой подход позволяет изменить возвращаемые значения функции и тем самым повлиять на поведение любого кода, который её вызывает. Конечно, можно комбинировать оба метода, но различие становится особенно важным, когда начинаешь работать с защищёнными (secure) перехватами.
  
  Post-hook реализовать немного сложнее стандартными способами. Сначала нужно вызвать оригинальную функцию, сохранить её результаты, изменить их, а потом вернуть изменённые данные. В первую очередь может показаться логичным сохранить результаты в таблицу, но это сложно, если функция возвращает nil значения.
  
  В следующих примерах показано два способа решения этой проблемы при создании перехвата функции. Оба примера работают как простое “хранилище“ для WoW. Функция GetMoney() перехватывается, и её результат изменяется.
  
  Когда вы открываете рюкзак, количество отображаемого золота будет отличаться от фактического количества у вас на руках. Величина изменения контролируется глобальной переменной SAVED_MONEY, которая вычитается из реальной суммы.
  
  Эти примеры не помешают вам тратить деньги из вашего “невидимого“ запаса; на самом деле, если у персонажа денег меньше, чем SAVED_MONEY [например, 1 золото в примере], отображаемая сумма в рюкзаке может стать абсурдной, поскольку интерфейс не рассчитан на отрицательные значения.
  
📘 Использование функции с переменным числом аргументов

  На самом деле вас интересует только первое возвращаемое значение функции GetMoney(), потому что в данный момент она возвращает только одно значение. Но можно создать пару функций, которые позволяют легко работать с этим значением, при этом сохраняя возможность возвращать и остальные значения, которые могут появиться в будущем.
  
  SAVED_MONEY = 10000
  local origGetMoney = GetMoney

  local function newGetMoney(realMoney, ...)
    return realMoney - SAVED_MONEY, ...
  end
  
  function GetMoney(...)
    return newGetMoney(origGetMoney(...))
  end
  
  Сначала вы определяете новую переменную SAVED_MONEY и сохраняете ссылку на оригинальную функцию GetMoney. Далее создаёте вспомогательную функцию, которая принимает возвращаемые значения оригинальной функции, изменяет первое, вычитая SAVED_MONEY, а остальные значения возвращает без изменений. Затем создаёте новую глобальную функцию GetMoney, которая вызывает вспомогательную функцию, передавая ей результаты вызова оригинальной.
  
📘 Использование вспомогательных функций “capture()“ и “release()“

  Следующий метод может потенциально использовать немного больше памяти и быть менее эффективным, но он обеспечивает удобный способ сохранить возвращаемые значения функции, чтобы их можно было проанализировать и, при необходимости, изменить. Суть метода — заменить значения nil специальным маркером, чтобы с ними можно было работать обычными средствами обработки таблиц. Функция capture() выглядит так:
    function capture(...)
      return { select("#", ...), ... }
    end

  capture() сохраняет результаты в таблицу, при этом добавляя количество аргументов в первый элемент этой таблицы. Это нужно для того, чтобы функция release() могла корректно вернуть все значения — даже если некоторые из них равны nil.
  
  Функция release() выглядит следующим образом:
    function release(tbl, index)
      local size = tbl[1]
      index = index or 2

      if index <= size then
        return tbl[index], release(tbl, index + 1)
      end
    end

  Извлечение аргументов немного сложнее, но по-прежнему довольно прямолинейно. Функция принимает второй аргумент, который по умолчанию равен 2 — это гарантирует, что при вызове release() вы получите всё, начиная со второго элемента (так как в первом хранится количество аргументов). Далее идёт проверка диапазона, и начинается рекурсивный возврат значений.
  
  Теперь эти функции можно использовать следующим образом:
    SAVED_MONEY = 10000
    local origGetMoney = GetMoney
    
    function GetMoney(...)
      local result = capture(origGetMoney(...))
      result[2] = result[2] - SAVED_MONEY
      return release(result)
    end
  
  Хотя оба метода могут показаться чрезмерно сложными, они дают наибольшую гибкость и надёжность в будущем. Хуки — это не то, что часто используется при разработке аддонов, и применять их следует с осторожностью.

📘 Хуки для скриптов виджетов
  
  Хуки часто используются при работе со скриптами фреймов. Например, ваш аддон может захотеть реагировать на нажатие какой-либо кнопки в стандартном интерфейсе, при этом не нарушая её обычного поведения. Рассмотрим кнопку Abandon Quest в журнале заданий, которая позволяет удалить задание. Сейчас она вызывает окно подтверждения, но никак не предупреждает, если задание уже завершено.
  
  Первый шаг при установке хука на скрипт фрейма — получить текущий обработчик скрипта с помощью метода GetScript:
  
    local origOnClick = QuestLogFrameAbandonButton:GetScript("OnClick")
    
  Метод GetScript() возвращает ссылку на текущую функцию-обработчик или nil, если обработчик не установлен. Это аналогично первой строке в предыдущих примерах.
  
  👉 Важно: 
    Всегда проверяйте, установлен ли уже обработчик скрипта, прежде чем его заменять, даже если кажется, что нужный фрейм не использует его по умолчанию. Возможно, другой аддон тоже работает с этой кнопкой, и слепая подмена скрипта сломает его работу. Если оригинальной функции нет, вы можете пропустить вызов оригинала в новой функции — но проверка обязательна.
  
  Далее создаём новую функцию-обработчик:

    local msg = "*** WARNING! YOU ARE ABOUT TO ABANDON A COMPLETED QUEST ***"
    
    local function newOnClick(...)
      local questIndex = GetQuestLogSelection()
      local completed = select(7, GetQuestLogTitle(questIndex))
      
      if completed then
        RaidNotice_AddMessage(RaidWarningFrame, msg, ChatTypeInfo["RAID_WARNING"])
      end

      if origOnClick then
        return origOnClick(...)
      end
    end
    
  Код для проверки и предупреждения довольно прост. Сначала вызывается GetQuestLogSelection() для получения текущего выбранного задания, затем GetQuestLogTitle() определяет, завершено ли оно. Если да — отображается предупреждение, а затем вызывается оригинальный обработчик, чтобы сохранить нормальное поведение кнопки.
  
  Теперь, когда у нас есть новая функция, её нужно установить в качестве обработчика события OnClick:
    QuestLogFrameAbandonButton:SetScript("OnClick", newOnClick)
    
📘 Безопасное (secure) перехватывание функций
  
  Как показано в Главе 15, вмешательство в переменные, используемые стандартным интерфейсом, может привести к множеству проблем из-за taint [загрязнения]. Вам может понадобиться перехватить (hook) какую-то, казалось бы, безобидную функцию для своего аддона, но если она участвует в защищённом (secure) коде, это может вызвать непредсказуемые последствия.
  
  Во многих случаях вы можете достичь цели, не изменяя поведение оригинальной функции. Для таких случаев World of Warcraft предоставляет функцию hooksecurefunc, которая создаёт безопасный post-hook — то есть хук, выполняющийся после вызова оригинальной функции. Такой хук не вызывает taint.
  
  Так как хук выполняется после оригинальной функции:
    ◽ вы не можете изменить её поведение;
    ◽ любые значения, возвращаемые вашим хуком, игнорируются, поэтому вы не можете повлиять на код, который вызвал функцию.
  
  Сигнатура функции hooksecurefunc:

    hooksecurefunc([table,] functionName, hookFunction)

  “Обратите внимание“: вы можете перехватывать только глобальные функции или методы объектов/таблиц. Например, следующий код выводит сообщение в чат каждый раз, когда вы нажимаете кнопку действия или вызывается функция UseAction:
  
    hooksecurefunc("UseAction", function(slot, unit)
      ChatFrame1:AddMessage(format(
        "You used action %d on %s",
        slot, unit or "<no unit>"
      ))
    end)
    
  Чтобы безопасно перехватить метод таблицы, нужно передать таблицу первым аргументом. Например, если вы хотите отслеживать вызовы метода Show у определённого фрейма:
  
  Хук на метод Show отличается от реакции на событие OnShow. Если фрейм уже показан, и какой-то код снова вызывает Show(), этот хук сработает, а скрипт OnShow — нет.
  
  👉 Пример:

    hooksecurefunc(someFrame, "Show", function(self)
      ChatFrame1:AddMessage("Attempting to show someFrame")
    end)
    
  В обоих этих примерах вы свободны в использовании параметров и возвращаемых значений, потому что:
    ◽ возвращаемые значения игнорируются;
    ◽ изменение аргументов не влияет на остальной код.
  
  Это значит, что вы можете организовать хук-функции так, как вам удобно.
  
📘 Безопасное перехватывание скриптов (Hooking Scripts Securely)

  Реагировать на вызов обработчика (handler) виджета, не изменяя его поведение, на самом деле проще, чем использовать “обычный“ метод перехвата, который вы видели ранее.
  
  Аналогично hooksecurefunc, каждый виджет имеет метод HookScript, который выполняет ту же задачу:
  👉 ваш хук будет вызван после оригинального скрипта с теми же аргументами, а любые возвращаемые значения будут проигнорированы.
  
  Пример: следующий код безопасно перехватывает обработчик OnClick у фрейма игрока, отслеживая количество нажатий:

    clickCounter = 0

    PlayerFrame:HookScript("OnClick", function()
      clickCounter = clickCounter + 1
      print(clickCounter, "clicks and counting...")
    end)

  Этот хук применяется только к указанной кнопке (если вы хотите отслеживать все безопасные нажатия кнопок, используйте hooksecurefunc на SecureActionButton_OnClick).
  
  👉 Вам даже не нужно проверять, установлен ли уже скрипт на обработчик — HookScript автоматически учтёт это за вас!
  
📘 Когда стоит использовать перехват функций (Deciding When to Hook)

  Хотя перехват функций — безусловно, полезный инструмент, у него есть несколько важных предостережений. Они варьируются от проблем с производительностью до возможного нарушения работы элементов стандартного интерфейса или других аддонов.
  
  Во многих случаях использование перехвата не требуется, и можно обойтись менее навязчивыми альтернативами.
  
📘 Понимание цепочки перехвата (Understanding the Hook Chain)

  Чтобы по-настоящему понять, какие проблемы могут возникнуть при небрежном использовании перехвата, представь, что произойдёт, если два аддона перехватят одну и ту же функцию.
  
  Возьмём для примера ранее рассмотренную функцию GetMoney. Когда какой-либо код вызывает GetMoney, сначала выполняется перехваченная версия из твоего аддона, а затем твой код вызывает оригинальную функцию. Если затем другой аддон тоже перехватывает GetMoney, добавляется ещё одно звено в цепочку. Такая цепочка перехвата — это способ представить взаимодействие нескольких аддонов и базовой функции.
  
  В этом случае цепочка выглядит так:
    Другой аддон -> Твой аддон -> Встроенная GetMoney
  
  Безопасные перехваты (через hooksecurefunc и HookScript) на самом деле не строят такую цепочку. Их проще представить как брелок с ключами, где каждый перехват — это отдельный ключ. После выполнения основной функции система проходит по каждому перехвату и запускает их независимо друг от друга.
  
  Однако это не делает безопасные перехваты неуязвимыми для описанных далее проблем.
  
📘 Нельзя полагаться на порядок (You Can't Rely on Order)
  
  Твой аддон никак не может узнать, в каком порядке будут вызываться перехваты.
  Если другой аддон загружен раньше твоего, цепочка вызовов будет выглядеть так:
    Твой аддон -> Другой аддон -> Встроенная GetMoney
  
  Никогда не рассчитывай, что твой аддон окажется в каком-то конкретном месте в цепочке. Порядок загрузки аддонов полностью управляется движком игры.
  
  Можно немного повлиять на это с помощью зависимостей (dependencies), но это требует заранее знать имена всех аддонов, которые могут повлиять на твою работу.
  
  Если ты столкнулся с ситуацией, в которой важен порядок, возможно, стоит пересмотреть архитектуру аддона.
  
📘 Нельзя “расперехватить“ функцию (There Is No Unhook)

  Цепочка перехватов вызывает ещё одну проблему: опасно пытаться “расперехватить“ функцию.
  В приведённом ранее примере, если ты решишь “удалить“ свой перехват, выполнив:

  GetMoney = origGetMoney

  👉 ты удалишь не только свой, но и чужой перехват, потому что переменная origGetMoney была сохранена до того, как другой аддон успел внедрить свой перехват.

  Более того, безопасные (secure) перехваты вообще невозможно удалить — в API нет механизма для этого.
  Если тебе всё же нужно отключить поведение перехвата, лучше использовать флаг внутри своей функции и просто проверять его. При необходимости ты можешь включить или отключить логику перехвата, не убирая сам хук.
  Но, конечно, это подводит к следующей потенциальной проблеме…
  
📘 Перехваты ухудшают производительность (Hooking Hits Performance)

  Каждый перехват функции добавляет ещё один уровень вложенности вызова. В псевдокоде это выглядит примерно так:

    LastHook(...)
      AnotherHook(...)
        SecondHook(...)
          FirstHook(...)
            OriginalFunction(...)
            return ...
          return ...
        return ...
      return ...
    return ...
    
  На каждом уровне Lua копирует аргументы, чтобы вызвать следующую функцию, а затем копирует возвращаемые значения при выходе назад по цепочке.
  
  В случае безопасных (secure) перехватов вложенность не создаётся, но копирование данных всё равно происходит на каждом шаге.
  
  Обычно это не создаёт проблем, но если ты перехватываешь функцию, которая вызывается много раз за кадр, это может заметно повлиять на производительность клиента.
  
📘 Поиск альтернатив (Finding Alternatives)

  Мы не хотим полностью отговорить тебя от использования перехватов — у них действительно есть своё применение. Однако прежде чем использовать hook, всегда стоит рассмотреть возможные альтернативы, чтобы избежать потенциальных проблем.
  
  В таблице ниже приведены частые сценарии, в которых твоей первой мыслью может быть “нужно перехватить“, а также возможные альтернативы, которые могут быть проще и безопаснее. Не стесняйся обращаться за советом к сообществу UI — более широкий опыт часто помогает найти лучшие решения, которые делают аддон эффективнее.
  
  OnShow 
    стандартного фрейма или фрейма другого аддона	Создай собственный дочерний фрейм с обработчиком OnShow. Когда основной фрейм показывается или скрывается, твой тоже отреагирует. Чтобы «отключить» hook, достаточно сменить родителя фрейма.

  MerchantFrame_OnShow, 
    чтобы отреагировать на открытие окна торговца	Зарегистрируйся на событие MERCHANT_SHOW и обрабатывай данные торговца напрямую. Для отмены hook — просто отмени регистрацию события. Это также предотвращает конфликты с пользовательскими окнами торговцев.

  SetAttribute, 
    чтобы отслеживать изменение атрибута фрейма	Вместо этого установи или перехвати обработчик OnAttributeChanged. Этот способ ближе по смыслу к тому, что ты хочешь сделать, чем перехват метода напрямую.
  
  👉 Вывод:
    Cтарайся использовать встроенные события и скрипты вместо прямых перехватов, когда это возможно. Это сделает аддон стабильнее, безопаснее и проще в поддержке.
    
📘 Проектирование аддона: MapZoomOut

  MapZoomOut — простой аддон, который, каждый раз при изменении игроком масштаба миникарты, запускает таймер, который через 20 секунд возвращает карту к максимальному удалению (full zoom out). Это позволяет временно менять масштаб, но гарантирует, что карта всегда будет в полном масштабе для удобства слежения и работы с профессиями.
  
  👉 Описание поведения:
    ◽Когда вызывается метод Minimap:SetZoom(zoomLevel) для изменения масштаба миникарты — запускается таймер.
    ◽Когда таймер достигает 20 секунд — масштаб меняется обратно на минимальный (полностью увеличенный обзор).
  
  👉 Детали реализации:
    ◽Метод Minimap:SetZoom(zoomLevel) изменяет масштаб миникарты.
    ◽Минимальный уровень масштаба — 0 (полностью увеличенный обзор, zoomed out).
    ◽Максимальный уровень можно получить вызовом Minimap:GetZoomLevels().
    ◽Две кнопки управления масштабом (плюс и минус) используют SetZoom() для изменения уровня.
    ◽Кнопки сами включают и отключают себя (например, кнопка уменьшения масштаба отключается при минимальном уровне 0).
    ◽Текущий уровень масштаба можно получить через GetZoom().
    ◽Для запуска таймера потребуется перехватить вызов метода SetZoom().
  
  👉 Структура аддона:
    ◽Аддон не требует XML-файлов.
    ◽Создайте базовую папку аддона.
    ◽Добавьте файл MapZoomOut.toc со следующим содержанием:
      ## Interface: 30300
      ## Title: MapZoomOut
      ## Notes: Zooms the map out to the full level after a given time
      MapZoomOut.lua

      В MapZoomOut.lua будет реализована логика перехвата и таймера.
      
📘 Создание таймерного фрейма
  
  Создайте новый файл с именем MapZoomOut.lua и добавьте в начало файла следующую строку:
    local timerFrame = CreateFrame("Frame")
    
  Эта команда создаёт фрейм без глобального имени и присваивает его локальной переменной, чтобы использовать его позже в файле. Этот фрейм будет использоваться для запуска таймера с помощью события OnUpdate.
  
  📘 Первоначальная настройка
    Добавь в конец файла MapZoomOut.lua эти строки — они создают переменную с задержкой таймера, счётчик времени и сохраняют ссылку на оригинальную функцию Minimap:SetZoom:
      local DELAY = 20
      local counter = 0
      local origSetZoom = Minimap.SetZoom
      
    Эти локальные переменные потом будут использоваться для отслеживания таймера и вызова оригинальной функции SetZoom.
    
  📘 Создание перехвата функции
    Новая функция проста: она вызывает timerFrame:Show() и сбрасывает счётчик в 0, чтобы запустить таймер. Затем вызывается оригинальная функция SetZoom для изменения уровня масштабирования. Добавьте следующее определение функции в конец файла MapZoomOut.lua:
  
      function Minimap.SetZoom(...)
        -- Показать таймер, запуская отсчёт
        timerFrame:Show()
        counter = 0
        -- Вызвать оригинальную функцию SetZoom
        return origSetZoom(...)
      end
    
    Параметры функции и её результат просто передаются оригинальной функции с помощью ..., а результат возвращается вызывающей стороне. Это гарантирует корректную работу функции с любыми вызовами, поскольку она дополняет поведение, а не заменяет оригинальное.
    
  📘 Написание кода таймера
    Таймер здесь чуть сложнее, чем в предыдущем примере, в основном по эстетическим причинам. Проще было бы просто вызвать origSetZoom(Minimap, 0) по истечении таймера, но это вызвало бы резкое изменение масштаба миникарты, что выглядит не очень приятно. Вместо этого функция будет постепенно уменьшать масштаб, создавая плавный эффект.
    
    Добавьте следующий код в конец файла MapZoomOut.lua:
   
      local function OnUpdate(self, elapsed)
        -- Увеличиваем счётчик
        counter = counter + elapsed
        
        if counter >= DELAY then
          -- Получаем текущий уровень масштабирования
          local z = Minimap:GetZoom()
  
          if z > 0 then
            origSetZoom(Minimap, z - 1)
          else
            -- Включаем/выключаем кнопки
            MinimapZoomIn:Enable()
            MinimapZoomOut:Disable()
            self:Hide()
          end
        end
      end
    
    В начале таймера происходит то же — увеличение счётчика и проверка с задержкой. Внутри условия текущий уровень масштабирования сохраняется в локальную переменную z для удобства. Если миникарта увеличена (z > 0), то масштаб уменьшается на один уровень, но счётчик не сбрасывается. Это гарантирует, что функция таймера выполнится и в следующем обновлении кадра. Именно это обеспечивает плавное уменьшение масштаба, а не резкий скачок за один кадр.
    
    Наконец, когда миникарта полностью уменьшена, кнопка увеличения масштаба MinimapZoomIn становится активной, кнопка уменьшения MinimapZoomOut — неактивной, и сам таймер скрывается, чтобы его функция OnUpdate больше не вызывалась.
    
  📘 Финальная настройка
    Последний шаг — это установка обработчика OnUpdate для timerFrame, а также определение того, что делать при первой загрузке аддона. Поскольку игрок может начать игру с уже увеличенной миникартой, код проверит, нужно ли запускать таймер или сразу его скрыть (чтобы он не выполнялся зря).
    
    Добавьте следующий код в конец файла MapZoomOut.lua:
    
      timerFrame:SetScript("OnUpdate", OnUpdate)
      
      if Minimap:GetZoom() == 0 then
        timerFrame:Hide()
      end
      
  📘 Тестирование MapZoomOut
    Как и с любым другим аддоном, важно протестировать его функциональность, чтобы убедиться, что всё работает корректно и без побочных эффектов. В данном случае следует проверить следующее:
      ◽Используйте кнопку увеличения масштаба, чтобы приблизить карту на каждый уровень, и убедитесь, что через DELAY секунд карта постепенно возвращается к обычному масштабу.
      ◽Вручную вызовите функцию Minimap:SetZoom() через команду:
      /script Minimap:SetZoom(3)
      
    Аддон должен отреагировать на изменение и запустить таймер, даже если кнопки не были нажаты.
    
    
📘 Резюме

  Хуки функций — это полезный инструмент в арсенале программиста, но к нему прилагается увесистая инструкция с множеством предостережений. При создании хуков функций следуйте следующим принципам:
    ◽Используйте те же параметры и возвращаемые значения.
    ◽Вызывайте оригинальную функцию и сохраняйте цепочку хуков.
    ◽Не изменяйте порядок или смысл аргументов и возвращаемых значений, так как другие функции могут на них полагаться.
    ◽Не рассчитывайте на определённое место своего хука в цепочке. Это также означает, что не следует «отвязывать» функцию (unhook).
    ◽Сначала ищите альтернативы — хук может быть не лучшим решением.
  
  🔥 Кроме того, при работе с хуками в скриптах и функциях Blizzard следует проявлять особую осторожность, поскольку некоторые действия разрешены только для интерфейса по умолчанию.